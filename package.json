{
	"name": "wc_batch_requests",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "npm run build:live",
		"build": "tsc -p .",
		"build:live": "nodemon --watch 'app/**/*.ts' --exec 'ts-node' app/index.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^24.0.1",
		"ts-node": "^10.9.2",
		"typescript": "^5.8.3"
	},
	"dependencies": {
		"dotenv": "^16.5.0",
		"nodemon": "^3.1.10"
	}
}
